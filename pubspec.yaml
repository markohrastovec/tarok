name: tarok
description: Trying to make a card game with flutter.

publish_to: 'none'

version: 0.0.0+1

environment:
  sdk: ^3.0.0
  flutter: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.11.0

flutter_intl:
  enabled: true